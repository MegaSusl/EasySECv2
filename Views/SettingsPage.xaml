<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="EasySECv2.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Настройки"
    BackgroundColor="{StaticResource PageBackground}">

    <ScrollView>
        <VerticalStackLayout Spacing="24" Padding="16">

            <!-- Существующий блок: очистка таблиц -->
            <Frame BackgroundColor="{StaticResource CardBackground}"
               CornerRadius="8"
               Padding="12"
               HasShadow="True">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Выберите таблицу:"
                       FontAttributes="Bold"
                       TextColor="{StaticResource TextPrimary}" />

                    <Picker x:Name="TablesPicker"
                        Title="Таблица"
                        TextColor="{StaticResource TextPrimary}"
                        BackgroundColor="{StaticResource AccentLight}" />
                </VerticalStackLayout>
            </Frame>

            <Button Text="Удалить все записи"
                BackgroundColor="#E53935"
                TextColor="White"
                CornerRadius="6"
                Padding="12,8"
                HorizontalOptions="Center"
                Clicked="OnDeleteAllClicked" />

            <Button Text="Заполнить направления из JSON"
                BackgroundColor="{StaticResource Primary}"
                TextColor="White"
                CornerRadius="6"
                Padding="12,8"
                HorizontalOptions="Center"
                Clicked="OnSeedOrientationsClicked" />
          
            <BoxView HeightRequest="1" BackgroundColor="Gray" />

            <Label Text="Выберите страницу:" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"/>
            <Picker x:Name="PagePicker"
                Title="Выберите страницу"
                SelectedIndexChanged="OnPagePickerChanged" />

            <Label Text="Маркеры по умолчанию:" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"/>
            <CollectionView x:Name="MappingsEditor" ItemsSource="{Binding GlobalMappings}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Orientation="Vertical" Padding="5">
                            <Label Text="Маркер" TextColor="{StaticResource TextPrimary}"/>
                            <Entry Text="{Binding Placeholder}" TextColor="{StaticResource TextPrimary}"/>

                            <Label Text="Источник" TextColor="{StaticResource TextPrimary}"/>
                            <Picker ItemsSource="{Binding BindingContext.SourceTypes, Source={x:Reference MappingsEditor}}" SelectedItem="{Binding SourceType}" />

                            <Label Text="Свойство (если нужно)" TextColor="{StaticResource TextPrimary}"/>
                            <Entry Text="{Binding Property}" TextColor="{StaticResource TextPrimary}"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button Text="Сохранить маркеры" Clicked="OnSaveMappingsClicked" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
